import { styled } from "@stitches/react";

export const VisualOutput = () => {
  return <VisualOutputContainer />;
};

const VisualOutputContainer = styled("div", {
  height: "100%",
  flexGrow: 1,
});
